/**
 * @format
 */

import {AppRegistry} from 'react-native';
import RNUrbanJourney from 'react-native-urban-journey';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => RNUrbanJourney);
